name: exploradores
description: Jovens Gênios - Exploradores

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  cupertino_icons: ^1.0.6

  get: ^4.6.6
  get_storage: ^2.1.1
  dio: ^5.3.2
  dartz: ^0.10.1
  url_strategy: ^0.2.0
  flutter_svg: ^2.0.7
  google_fonts: ^6.0.0
  intl: ^0.18.1
  bcrypt: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
